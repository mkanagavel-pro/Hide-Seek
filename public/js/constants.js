/**
 * Client-side Constants
 * Must stay in sync with server/constants.js
 */

const CONSTANTS = Object.freeze({
  MAP_WIDTH:       1600,
  MAP_HEIGHT:      1200,
  TAG_DISTANCE:    36,
  GAME_DURATION:   180,   // seconds
  PLAYER_SPEED:    3.2,   // pixels per frame
  PLAYER_RADIUS:   16,
  NAME_TAG_OFFSET: 28,
  SEND_RATE:       50,    // ms between position sends
});
